import { CommonModule } from '@angular/common';
import { HttpParams } from '@angular/common/http';
import { ChangeDetectorRef, Component, OnInit, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { finalize, forkJoin } from 'rxjs';
import { ApiService } from '../../../core/services/api.service';
import { AppIconComponent } from '../../../shared/components/app-icon/app-icon';
interface Gym{gymId:number;name:string;location:string;capacity:number;description:string;isActive:boolean;}interface Slot{slotId:number;gymId:number;slotDate:string;startTime:string;endTime:string;maxCapacity:number;bookedCount:number;remainingCapacity:number;isAvailable:boolean;requiresPayment:boolean;feeAmount:number;}interface Availability{slots:Slot[];}interface Booking{bookingId:number;userId:number;gymId:number;gymName:string;bookingDate:string;startTime:string;endTime:string;status:string;requiresPayment:boolean;feeAmount:number;paymentStatus:string;}interface Student{studentId:number;userId?:number|null;firstName:string;lastName:string;masterStudentId:number;}interface Master{masterStudentId:number;universityStudentId:string;}
@Component({selector:'app-admin-gym',standalone:true,imports:[CommonModule,FormsModule,AppIconComponent],templateUrl:'./admin-gym.html'})
export class AdminGymComponent implements OnInit{private readonly api=inject(ApiService);private readonly cdr=inject(ChangeDetectorRef);gyms:Gym[]=[];bookings:Booking[]=[];students:Student[]=[];masters:Master[]=[];selected?:Gym;slots:Slot[]=[];date=new Date().toISOString().slice(0,10);tab:'overview'|'slots'|'bookings'='overview';loading=true;detailLoading=false;saving=false;error='';success='';search='';gymModal=false;slotModal=false;gymForm:any={name:'',location:'',capacity:30,description:''};slotForm:any={slotDate:'',startTime:'08:00',endTime:'09:00',maxCapacity:20,requiresPayment:false,feeAmount:0};ngOnInit():void{this.load();}load():void{this.loading=true;forkJoin({gyms:this.api.get<Gym[]>('Gym'),bookings:this.api.get<Booking[]>('Gym/bookings'),students:this.api.get<Student[]>('Student'),masters:this.api.get<Master[]>('StudentMasterList')}).pipe(finalize(()=>{this.loading=false;this.cdr.markForCheck();})).subscribe({next:r=>{this.gyms=r.gyms;this.bookings=r.bookings;this.students=r.students;this.masters=r.masters;},error:e=>this.error=this.readError(e,'Unable to load gym management.')});}get filtered():Gym[]{const q=this.search.toLowerCase().trim();return this.gyms.filter(g=>!q||`${g.name} ${g.location}`.toLowerCase().includes(q));}openGym(g:Gym):void{this.selected=g;this.tab='overview';this.loadSlots();}back():void{this.selected=undefined;this.slots=[];}loadSlots():void{if(!this.selected)return;this.detailLoading=true;const params=new HttpParams().set('date',this.date);this.api.get<Availability>(`Gym/${this.selected.gymId}/availability`,params).pipe(finalize(()=>{this.detailLoading=false;this.cdr.markForCheck();})).subscribe({next:a=>this.slots=a.slots,error:e=>this.error=this.readError(e,'Unable to load gym slots.')});}get selectedBookings():Booking[]{return this.selected?this.bookings.filter(b=>b.gymId===this.selected!.gymId):[];}studentLabel(userId:number):string{const s=this.students.find(x=>x.userId===userId);if(!s)return'Active student';const m=this.masters.find(x=>x.masterStudentId===s.masterStudentId);return`${s.firstName} ${s.lastName}${m?.universityStudentId?' · '+m.universityStudentId:''}`;}createGym():void{this.saving=true;this.api.post<Gym>('Gym',this.gymForm).pipe(finalize(()=>{this.saving=false;this.cdr.markForCheck();})).subscribe({next:g=>{this.gyms=[...this.gyms,g];this.gymModal=false;this.success='Gym created.';},error:e=>this.error=this.readError(e,'Unable to create gym.')});}addSlot():void{if(!this.selected)return;this.saving=true;const body={gymId:this.selected.gymId,slotDate:this.slotForm.slotDate,startTime:this.slotForm.startTime,endTime:this.slotForm.endTime,maxCapacity:Number(this.slotForm.maxCapacity),requiresPayment:!!this.slotForm.requiresPayment,feeAmount:this.slotForm.requiresPayment?Number(this.slotForm.feeAmount):0};this.api.post<Slot>('Gym/slots',body).pipe(finalize(()=>{this.saving=false;this.cdr.markForCheck();})).subscribe({next:()=>{this.slotModal=false;this.success='Gym slot created.';this.loadSlots();},error:e=>this.error=this.readError(e,'Unable to create gym slot.')});}complete(b:Booking):void{this.saving=true;this.api.post<Booking>(`Gym/bookings/${b.bookingId}/complete`,{}).pipe(finalize(()=>{this.saving=false;this.cdr.markForCheck();})).subscribe({next:u=>{this.bookings=this.bookings.map(x=>x.bookingId===u.bookingId?u:x);this.success='Gym booking marked completed.';},error:e=>this.error=this.readError(e,'Unable to complete gym booking.')});}dateLabel(v:string):string{return new Date(v).toLocaleDateString([], {day:'2-digit',month:'short',year:'numeric'});}time(v:string):string{const[h,m]=v.slice(0,5).split(':').map(Number);const d=new Date();d.setHours(h||0,m||0);return d.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'});}private readError(e:any,f:string):string{return e?.error?.message??(typeof e?.error==='string'?e.error:f);}}
